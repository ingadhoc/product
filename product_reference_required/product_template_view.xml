<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record model="ir.ui.view" id="product_inherit_form_view">
        <field name="name">product.template.inherit.form.view</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <!-- when we deactivate a product.template de default code gets false value because all variants are deactivated, we make it not required in this case so that user can re-activate de product.template -->
            <field name="default_code" position="attributes">
                <attribute name="attrs">{'invisible': [('product_variant_count', '&gt;', 1)], 'required': [('active', '=', True)]}</attribute>
            </field>
        </field>
    </record>
</odoo>
