<?xml version="1.0"?>
<odoo> 
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="search_view_id" ref="estate.estate_property_view_search"/>
        <field name="context">{'search_default_available': True}</field>
    </record>

    <!-- estate.property tree view -->
    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">estate.property.view.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree decoration-success="state in ('offer_received','offer_accepted')" 
                decoration-bf="state=='offer_accepted'"
                decoration-muted="state=='sold'"
                decoration-danger="state=='canceled'">
                <field name="state"/> 
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="True"/>
            </tree>
        </field>
    </record>

    <!-- estate.property form view -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.view.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button name="action_sell_property" type="object" string="Sold" invisible="state in ('sold','canceled','offer_accepted')"/>
                    <button name="action_sell_property" type="object" class="btn-primary" string="Sold" invisible="state != 'offer_accepted'"/>
                    <button name="action_cancel_property" type="object" string="Cancel" invisible="state in ('sold','canceled')"/> 
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold" options="{'clickable': '1'}" />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" options="{'line_breaks': False}" widget="text" class="field_name" placeholder="e.g. 221B Baker Street Apartment"/></h1>
                    </div>
                    <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    <group class="mt-4"> 
                        <group>
                            <field name="property_type_id" options="{'no_create_edit': true}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="best_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ('offer_accepted','sold','canceled')"/> 
                        </page>
                        <page string="Other info">
                            <group>
                                <field name="partner_id"/>
                                <field name="user_id"/>
                            </group>    
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- estate.property search view -->
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search Description">
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area"
                    filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>

                <filter string="Available" name="available" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>
                <group expand="1" string="Group By">
                    <filter name="postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>
    <!-- estate.property kanban view -->
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">model.name.view.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban string="Properties Kanban" default_group_by="property_type_id" records_draggable="False">
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="oe_kanban_details">
                                <strong class="o_kanban_record_title"><field name="name"/></strong>
                            </div>
                            <field name="state" invisible="1"/> 
                            <div class="o_kanban_record_subtitle">
                                <p>
                                    Expected price: $<field name="expected_price"/>
                                </p> 

                                <div t-if="record.state.raw_value == 'offer_received'">
                                    <p>
                                        Best offer: $<field name="best_price"/>
                                    </p>
                                </div>
                                <div t-if="record.state.raw_value == 'offer_accepted'">
                                    <p>
                                        Selling price: $<field name="selling_price"/>
                                    </p>
                                </div>
                                <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            </div>
                        </div>
                    </t>
                </templates>    
            </kanban>
        </field>
    </record>

</odoo>